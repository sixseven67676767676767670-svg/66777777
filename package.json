{
  "name": "lua-script-protect",
  "version": "1.0.0",
  "description": "API que armazena scripts Lua e libera o raw apenas para executores (nao para navegadores)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
